# @package _global_
data:
  # General config
  n_training_samples: null
  dir: E:/shared_resources/coercion/flattened_datasets
  suffix: parquet

  # Feature specs
  pred_prefix: pred_
  outc_prefix: outcome_coercion_bool_within_7

  splits_for_training: ["train"]
  splits_for_evaluation: ["val"]

  col_name:
    pred_timestamp: timestamp
    outcome_timestamp: timestamp_outcome
    id: dw_ek_borger
    age: pred_age_in_years
    exclusion_timestamp: 
    custom_columns: 
    - outcome_coercion_type_within_2_days 
    - adm_id