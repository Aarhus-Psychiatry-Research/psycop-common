name: psycop-t2d-integration-testing
seed: 42

wandb:
  mode: disabled # Which mode to run WanDB in. Takes "run", "dryrun", and "disabled"
  group: integration_testing
  entity: psycop # Which entity to run WanDB in.

gpu: false
